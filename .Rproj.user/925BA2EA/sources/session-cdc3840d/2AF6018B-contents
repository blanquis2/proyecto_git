
                       #######Ecuación cuadrática#######
                       
                       

a<-as.numeric(readline(prompt="De tu ecuació tipo ax2 + bx + c = 0, ¿Cuál es el valor de a?")) #Saber cuál es el valor de a# 
#con as. numeric le indicamos que lo lea como un valor numérico#
a#imprimo para confirmar que si se regitraron mis valores#

b<-as.numeric(readline(prompt="¿Cuál es el valor de b?")) #Valor de b, usamos el promt para quien ejecute el script-#
#ponga los valores que prefiera#
b

c<-as.numeric(readline(prompt="¿Cuál es el valor de c?")) #Último valor, c#
c

#Tomaremos la fórmula general para resolver la ecuación#
#Sabiendo que hay dos soluciones posibles para cada ecuación cudrática, hagamos#
#dos objetos para cada una de ellas#

solucion1<-(-b)+ #Cuando se suma#
  (sqrt(abs(b^2-4*a*c))#usamos abs() para que tome como absoluto el resultado y no de NANs
  /(2*a))
solucion1

solucion2<-(-b)- #Cuando se resta#
  (sqrt(abs(b^2-4*a*c))#usamos abs() para que tome como absoluto el resultado y no de NANs
   /(2*a)) 
solucion2

#***El abs() solo es para que cuando corra el script no marque NaNs, de todos modos la discriminante#
#indicará si se puede o no resolver#

#Para saber si las soluciones son reales, usaremos el valor de la discriminante(D)#

d<-b^2-4*a*c
d

#Vamos a usar la condicional if e if else para señalar qué hacer, dependiendo de un resultado anterior#


if(d>0){
  print(paste("Ambas soluciones son reales:", solucion1,solucion2))
}else if (d<0){
  print("No hay solución en los números reales")
  print("La raíz pertenece a los números complejos, es decir, la solución de compone de un imaginario y reales")
}else if (d == 0){
  print(paste("Solo hay una solución para la ecuación", solucion1))
  #Uso de paste para unir 2 elemtos distintos, texto y objeto
  print("La raiz de cada una de las soluciones es la misma")
}


                       